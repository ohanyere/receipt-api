apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: receipt-api
  labels:
    app.kubernetes.io/name: receipt-api
spec:
  hosts:
    - receipt-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: receipt-api
            port:
              number: 80
          weight: 100
